[project]
name = "fde-lab"
version = "0.1.0"
description = "Hands-on lab for the SAP Forward Deployed Application/ML curriculum"
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.0",
    "langchain-google-genai>=4.0",
    "langgraph>=1.0",
    "python-dotenv>=1.0",
    "pydantic>=2.0",
    "rich>=13.0",
]


[tool.uv]
package = false
